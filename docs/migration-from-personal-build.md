# 从个人脚本版本迁移

旧的个人版本通常包含固定用户名、盘符、WSL 发行版、项目目录和模型映射。

迁移建议：

1. 先保留旧目录备份；
2. 停止旧的 7550/7551 进程；
3. 运行本仓库 `Install.cmd`；
4. 将旧映射手工复制到 `config/bridge.local.json`；
5. 不要复制旧日志、认证文件或 `.claude/settings.json`；
6. 使用诊断快捷方式验证端口和日志。
